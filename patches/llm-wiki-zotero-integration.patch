diff --git a/src-tauri/src/commands/mod.rs b/src-tauri/src/commands/mod.rsindex 911dc61..7be64aa 100644--- a/src-tauri/src/commands/mod.rs+++ b/src-tauri/src/commands/mod.rs@@ -7,3 +7,4 @@ pub mod fs; pub mod project; pub mod search; pub mod vectorstore;+pub mod zotero;diff --git a/src-tauri/src/lib.rs b/src-tauri/src/lib.rsindex 04a5702..666e441 100644--- a/src-tauri/src/lib.rs+++ b/src-tauri/src/lib.rs@@ -256,6 +256,9 @@ pub fn run() {             commands::file_sync::get_file_change_queue,             commands::file_sync::retry_file_change_task,             commands::file_sync::ignore_file_change_task,+            commands::zotero::zotero_start,+            commands::zotero::zotero_bbt_rpc,+            commands::zotero::zotero_local_api_get,             set_proxy_env,             set_close_behavior,         ])diff --git a/src/components/sources/sources-view.tsx b/src/components/sources/sources-view.tsxindex 177d69d..f6468db 100644--- a/src/components/sources/sources-view.tsx+++ b/src/components/sources/sources-view.tsx@@ -1,4 +1,4 @@-import { useState, useEffect, useCallback, useMemo, useRef } from "react"+﻿import { useState, useEffect, useCallback, useMemo, useRef } from "react" import { open } from "@tauri-apps/plugin-dialog" import { Plus, FileText, RefreshCw, BookOpen, Trash2, Folder, ChevronRight, ChevronDown } from "lucide-react" import { Button } from "@/components/ui/button"@@ -21,6 +21,7 @@ import { } from "@/lib/source-lifecycle" import { filterRawSourceTree } from "@/lib/source-filter" import { refreshProjectFileTree } from "@/lib/project-file-tree-refresh"+import { importZoteroLibrary } from "@/lib/zotero-import"  const SOURCE_TREE_INITIAL_ROWS = 160 const SOURCE_TREE_LOAD_BATCH = 160@@ -36,6 +37,7 @@ export function SourcesView() {   const dataVersion = useWikiStore((s) => s.dataVersion)   const [sources, setSources] = useState<FileNode[]>([])   const [importing, setImporting] = useState(false)+  const [zoteroImporting, setZoteroImporting] = useState(false)   const [ingestingPath, setIngestingPath] = useState<string | null>(null)   const [refreshing, setRefreshing] = useState(false)   const [refreshError, setRefreshError] = useState<string | null>(null)@@ -44,7 +46,7 @@ export function SourcesView() {    * confirm delete" state. Lifted up here (rather than living    * inside SourceTree) for two reasons:    *   1. Only one button can be armed at a time across the whole-   *      tree — clicking another delete disarms the prior one.+   *      tree 鈥?clicking another delete disarms the prior one.    *      Lifting state to the common ancestor makes that natural.    *   2. The auto-disarm timer (5s) needs to survive across re-    *      renders triggered by tree mutation; useEffect cleanup@@ -166,6 +168,34 @@ export function SourcesView() {     }   } +  async function handleImportZotero() {+    if (!project || zoteroImporting) return++    setZoteroImporting(true)+    try {+      const summary = await importZoteroLibrary(project, llmConfig, {+        startIfUnavailable: true,+      })+      await loadSources()+      window.alert(+        t("sources.zoteroImportComplete", {+          items: summary.itemsFound,+          metadataRemoved: summary.metadataFilesRemoved,+          attachments: summary.attachmentsImported,+          notes: summary.notesImported,+          alreadyPresent: summary.attachmentsAlreadyPresent,+          missingOnDisk: summary.attachmentsMissingOnDisk,+          queued: summary.ingestQueued,+        }),+      )+    } catch (err) {+      console.error("Failed to import Zotero library:", err)+      window.alert(t("sources.zoteroImportFailed", { error: String(err) }))+    } finally {+      setZoteroImporting(false)+    }+  }+   async function handleOpenSource(node: FileNode) {     try {       const content = await readFile(node.path)@@ -184,7 +214,7 @@ export function SourcesView() {     // confirmed via the inline UI, so we proceed unconditionally.     try {       const result = await deleteSourceFile(pp, node.path)-      // Step 8: Refresh everything (UI side — must run with parent+      // Step 8: Refresh everything (UI side 鈥?must run with parent       // context, hence kept here rather than inside the helper).       await loadSources()       await refreshProjectFileTree(pp, {@@ -208,7 +238,7 @@ export function SourcesView() {    * wiki-cascade for every individual file inside (so any    * derived wiki pages, embeddings, log entries get cleaned up    * the same way as a single-file delete), then removes the-   * folder itself with `deleteFile` — which dispatches to+   * folder itself with `deleteFile` 鈥?which dispatches to    * `remove_dir_all` Rust-side, taking the now-empty (or near-    * empty) directory tree with it including any leftover dotdir    * cache files we didn't explicitly target.@@ -243,7 +273,7 @@ export function SourcesView() {     if (!project || ingestingPath) return     // Re-ingest goes through the same automated queue path as a fresh     // import (`handleImport` above). Earlier this used `startIngest`,-    // which opens an interactive chat → user clicks "Save to Wiki" →+    // which opens an interactive chat 鈫?user clicks "Save to Wiki" 鈫?     // `executeIngestWrites`. That had two problems: (a) it duplicated     // the auto-pipeline so features like image cascade had to be     // wired in twice, and (b) the interactive flow surprised users@@ -290,6 +320,10 @@ export function SourcesView() {             <Plus className="mr-1 h-4 w-4" />             {t("sources.importFolder", "Folder")}           </Button>+          <Button size="sm" variant="outline" onClick={handleImportZotero} disabled={zoteroImporting}>+            <BookOpen className="mr-1 h-4 w-4" />+            {zoteroImporting ? t("sources.importingZotero") : t("sources.importZotero")}+          </Button>         </div>       </div> @@ -315,6 +349,10 @@ export function SourcesView() {                 <Plus className="mr-1 h-4 w-4" />                 {t("sources.importFolder")}               </Button>+              <Button variant="outline" size="sm" onClick={handleImportZotero} disabled={zoteroImporting}>+                <BookOpen className="mr-1 h-4 w-4" />+                {zoteroImporting ? t("sources.importingZotero") : t("sources.importZotero")}+              </Button>             </div>           </div>         ) : (@@ -417,7 +455,7 @@ function SourceTree({   onDeleteFolder: (node: FileNode) => void   /** Path of the node currently in "click again to confirm" state.    *  Lifted to the parent so only ONE button is armed at a time-   *  across the whole tree — clicking another delete arms that one+   *  across the whole tree 鈥?clicking another delete arms that one    *  and disarms the previous. */   pendingDeletePath: string | null   setPendingDeletePath: (path: string | null) => void@@ -566,7 +604,7 @@ function SourceTree({  /**  * Two-stage delete button. Default = ghost trash icon (subtle).- * Armed = solid red "Confirm" pill with the icon — visually+ * Armed = solid red "Confirm" pill with the icon 鈥?visually  * unmistakable, so the user can't miss the second-click warning.  *  * Same component is used for both files and folders; the parentdiff --git a/src/i18n/en.json b/src/i18n/en.jsonindex e07d387..2c4dbd6 100644--- a/src/i18n/en.json+++ b/src/i18n/en.json@@ -101,6 +101,8 @@     "importHint": "Import documents to start building your wiki",     "importFiles": "Import Files",     "importFolder": "Folder",+    "importZotero": "Zotero",+    "importingZotero": "Importing Zotero...",     "importSourceFiles": "Import Source Files",     "importSourceFolder": "Import Source Folder",     "refresh": "Refresh",@@ -109,6 +111,8 @@     "refreshFolderHint": "Rescan the source folder and process new, changed, or deleted files.",     "refreshFolderTooltip": "Rescan the project source folder with the same rules as automatic monitoring. New or modified allowed files are queued for ingest; deleted sources trigger the normal wiki and index cleanup; then the file tree is rebuilt.",     "refreshFailed": "Failed to refresh sources: {{error}}",+    "zoteroImportComplete": "Zotero import complete.\n\nItems scanned: {{items}}\nOld metadata MD removed: {{metadataRemoved}}\nFiles imported (PDF/MD): {{attachments}}\nZotero notes imported: {{notes}}\nAlready present: {{alreadyPresent}}\nSkipped (file missing on disk): {{missingOnDisk}}\nQueued for ingest: {{queued}}",+    "zoteroImportFailed": "Zotero import failed: {{error}}",     "ingest": "Ingest",     "delete": "Delete source and related wiki pages",     "deleteConfirm": "Delete \"{{name}}\" and its related wiki pages?\n\nThis will:\n- Delete the source file\n- Delete wiki pages generated from this source\n- Update index.md",diff --git a/src/i18n/zh.json b/src/i18n/zh.jsonindex c5dc0ee..f337467 100644--- a/src/i18n/zh.json+++ b/src/i18n/zh.json@@ -101,6 +101,8 @@     "importHint": "导入文档，开始构建你的 Wiki",     "importFiles": "导入文件",     "importFolder": "文件夹",+    "importZotero": "Zotero",+    "importingZotero": "正在导入 Zotero...",     "importSourceFiles": "导入源文件",     "importSourceFolder": "导入源文件夹",     "refresh": "刷新",@@ -109,6 +111,8 @@     "refreshFolderHint": "重新扫描资料文件夹，并处理新增、修改或删除的文件。",     "refreshFolderTooltip": "按自动监控相同规则重新扫描当前项目的资料文件夹。允许的新增或修改文件会加入提取队列；已删除的资料会走正常的 Wiki 与索引清理流程；随后重建文件树。",     "refreshFailed": "刷新原始资料失败：{{error}}",+    "zoteroImportComplete": "Zotero 导入完成。\n\n扫描条目：{{items}}\n已删除旧元数据 MD：{{metadataRemoved}}\n新导入附件（PDF/MD）：{{attachments}}\nZotero 笔记：{{notes}}\n已存在：{{alreadyPresent}}\n跳过（本地文件不存在）：{{missingOnDisk}}\n已加入提取队列：{{queued}}\n\n说明：跳过的附件是 Zotero 库中有记录但本机 storage 目录中找不到对应文件，需在 Zotero 中重新下载或同步附件。",+    "zoteroImportFailed": "Zotero 导入失败：{{error}}",     "ingest": "提取到 Wiki",     "delete": "删除资料及相关 Wiki 页面",     "deleteConfirm": "删除「{{name}}」及其相关 Wiki 页面？\n\n此操作将：\n- 删除原始资料文件\n- 删除由该资料生成的 Wiki 页面\n- 更新 index.md",